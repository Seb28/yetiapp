# Yetiapp Démo

## TLDR;

* `npm install` (installs lerna)
* `lerna bootstrap` (connects all the packages)
* `lerna exec npm run start` 

* Tree Structure

* `packages`
  * `api` (`deps: []`)
  * `webapp` (`deps: []`)

## Summary