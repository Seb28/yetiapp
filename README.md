# Yetiapp Démo

## TLDR;

* `npm install` (installs lerna)
* `lerna bootstrap` (connects all the packages)
* `npm -C ./packages/usage run start` 

* Tree Structure

* `packages`
  * `api` (`deps: []`)
  * `webapp` (`deps: []`)

## Summary